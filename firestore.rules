rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All application data is server-mediated through authenticated + App Check
    // protected callable functions. Mobile clients get no direct Firestore access.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
